i want to start to make the @mods/dotfiles/nvim/lua/user/opencode.lua opencode nvim plugin and @mods/dotfiles/nvim/lua/user/plugins/ai/sidekick.lua sidekick nvim plugin used in a mutually exclusive way within neovim. I want to create a command called AIToggle that makes the <leader>o key control either sidekick or opencode.nvim. Then i want to work on aligning the keybindings are the same regardless of the plugin being used. ie if AIToggle is set to sideick then <leader>o should open sidekick otherwise, if AIToggle is opencode.nvim then <leader>o should 
