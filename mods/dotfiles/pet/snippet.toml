[[snippets]]
  description = "docker kill all"
  command = "docker kill `docker ps -q`"
  tag = ["docker", "kill", "all"]
  output = ""

[[snippets]]
  description = "kill all chrome pids"
  command = "ps -ef | grep -i [c]hrom | awk '{print $2}' | xargs kill -9"
  tag = ["kill", "chrome", "chromium", "prerender", "all"]
  output = ""

[[snippets]]
  description = "sample terminal colors"
  command = "msgcat --color=test"
  output = ""

[[snippets]]
  description = "chat ai open neovim to chat with gpt models"
  command = "chatai"
  tag = ["chat", "ai", "gpt", "openai", "neovim"]

[[snippets]]
  description = "chat ai open neovim to chat with gpt models"
  command = "git for-each-ref --sort=-committerdate refs/heads/ --format='%(committerdate:short) %(refname:short)'"
  tag = ['git', 'branch', 'date', 'sort']

[[snippets]]
  description = "search for nix package"
  command = "nix-search --details <package>"
  tag = ['nix', 'search', 'package']

